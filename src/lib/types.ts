/**
 * Partner app domain types — foundation scope only.
 * Order types live in `types-orders.ts` (Feature #005/#006). Rider workflow
 * types land with Feature #008.
 */

export type PartnerRole = "cook" | "rider" | "picker";
